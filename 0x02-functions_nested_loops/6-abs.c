#include "main.h"
/**
*main - read code
*Description: compute absolute integer
*@n: integer argument
*Return: absolute values
*/
int _abc(int n)
{
if (n < 0)
{
return (n);
}
else if (n > 0)
{
return (-n);
}
return (0);
}
